using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class code : MonoBehaviour
{
    string gio = "hot day";
    float floatValue = 0.3f;
    int intValue = 4;
    int anotherIntValue = 26;
    float anotherFloatValue = 0.7f;
    bool trueValue = true;
    bool falseValue = false;
    
    // Start is called before the first frame update
    void Start()
    {
        // Here you can add code that should run at the start of the game.
    }

    // Update is called once per frame
    void Update()
    {
        // Here you can add code that should run every frame.
    }
}
